/*
In the first-expressions.js file in the code editor below, write an expression that:
    uses at least 3 different arithmetic operators.
    equals 42.

Hint: +, -, *, /,* and % are possible arithmetic operators*
*/

console.log((12 % 5) * (30 - 10) + (10 / 5))